{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to LogicApp",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureLogicAppsStandard.pickProcess}"
        }
    ]
}